过程调用练习
功能:接收年、月、日信息并进行显示。
目的:掌握响铃符方法;掌握年、月、日的输入方法;掌握过程调用方法。
内容:先显示提示信息“WHAT IS THE DATE (MM/DD/YY) ?”并响铃- - 次，
然后接收键盘输入的月1日1年信息，并显示。

参考流程图如下所示:
[段寄存器和堆栈初始化]
显示“WHAT IS THE DATE?”
输出响铃字符
调用GetNum,接收键入的月、面年值]
调用Disp显示年值」
输出字符'-'
[ 调用Dispp显示月值]
输出字符'-'
[调用Dispp显示日值]
返回DOS
